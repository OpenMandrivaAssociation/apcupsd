--- configure~	2010-09-02 10:23:39.000000000 -0400
+++ configure	2012-01-27 17:09:15.122811630 -0500
@@ -11711,13 +11711,15 @@
    SUPC="`$CXX -print-file-name=libsupc++.a 2>/dev/null`"
    case "$SUPC" in
    libsupc++.a*)
-      # Library not found, so this is and older GCC...
+      # Library not found, so this is an older GCC...
+      LD="$CXX"
       echo "$as_me:$LINENO: result: no" >&5
 echo "${ECHO_T}no" >&6
       ;;
    *)
       # This is gcc 3.x, and it knows of libsupc++, so we need it
       LIBS="$LIBS -lsupc++"
+      LD="$CC"
       echo "$as_me:$LINENO: result: yes" >&5
 echo "${ECHO_T}yes" >&6
 
